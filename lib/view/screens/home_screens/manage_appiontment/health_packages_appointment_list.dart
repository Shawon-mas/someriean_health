import 'package:flutter/material.dart';
class HealthPackagesAppointmentList extends StatelessWidget {
  const HealthPackagesAppointmentList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Health Packages Appointment List'),
      ),
    );
  }
}
